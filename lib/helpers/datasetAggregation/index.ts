/**
 * Dataset aggregation is independent of MapLibre rendering. This public module
 * is the stable entry point while the implementation is split by domain.
 */
export { DatasetAggregator } from "../mapManager/statsCalculator";
